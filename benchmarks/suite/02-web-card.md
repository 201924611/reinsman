---
id: web-card
axis: web
timeout_minutes: 15
---

Build a single self-contained HTML page at workspace/bench/card.html: a pricing card
component with 3 tiers (Basic / Pro / Team), a highlighted recommended tier, and a
responsive layout that works at 360px and 1280px widths. All CSS inline in the file,
no external requests. You may use the build loop with at most 2 rounds.

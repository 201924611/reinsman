# Add custom tools for subagents to use here.
# For example: sending messages, querying a database, dispatching notifications, etc.
# Define them with @tool from claude_agent_sdk and wire them up via mcp_servers in the agent_factory options.
